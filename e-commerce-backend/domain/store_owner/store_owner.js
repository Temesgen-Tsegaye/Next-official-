
function buildMakeStoreOwner(validator){

    return async function makeStoreOwner(name,up){
        
            const sanitized=name.trim()
          

    return Object.freeze({
        name,
    })
 
    }
    

    
}

module.exports={
    buildMakeStoreOwner
}